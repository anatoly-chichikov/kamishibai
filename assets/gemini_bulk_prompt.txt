You are refining the Kamishibai understanding list after a user comment. The original list was produced by the intake pass; the user has glanced at it and now points out something that needs to change across the whole batch (a sense choice, a register tweak, an extra item to drop, etc).

Target language (already chosen by the intake pass — do not change it):
{target_language}

Support language for explanations:
{support_language}

Apply the user's correction to the whole list. Keep rows that should not be turned into cards with `"ok": false` and explain why in their `understanding`. Output every row from the input — even untouched ones — in the same order.

Each item has three fields.

1. "term" — the surface form to learn. Correct typos silently. Preserve the user's morphological form unless the user's correction explicitly asks otherwise.

2. "understanding" — exactly one sentence in {support_language}, between 6 and 14 words, written for a human reader. State directly what the term means in the chosen sense.

   Hard rules for "understanding":
   - **Do not start with «Учим», «Изучаем», «Учимся», «We learn», «Studying» or any other narration of the learning act.** Skip straight to the meaning.
   - **Pick exactly one sense.** When the term is polysemous and the user did not pin a sense — pick the most common modern sense in {target_language} and stop. Never write "X или Y", "X or Y", "может означать X, а также Y".
   - Reflect any sense, register, or focus the user named in their correction.
   - **Stay tight to the meaning the learner needs.** Do not pile on incidental detail (appearance, behaviour anecdotes, common similes). No "ловит мышей и крыс", no "красный как помидор", no "пушистое млекопитающее".
   - Mention part of speech, idiom, or register only when it actually clarifies — never as a label-only line.
   - **Always abbreviate part-of-speech labels** in whichever support language you use — write the standard dictionary short form, never the full word. Russian: «сущ.», «прил.», «гл.», «нар.», «мест.», «предл.», «част.», «межд.», «числ.» (союз stays). English: «n.», «v.», «adj.», «adv.», «pron.», «prep.», «conj.», «interj.». Spanish: «sust.», «adj.», «v.», «adv.», «pron.». Do the same in any other support language. Never spell out «существительное», «прилагательное», «глагол», «наречие», «noun», «verb», «adjective», «adverb», etc.
   - Never use filler words: "обычное", "обычный", "повседневное", "стандартное", "обыкновенное", "regular", "common", "ordinary", "everyday", "standard", "basic", "simple".
   - Never produce a structured "POS · gloss · marker" line. Write a real, scannable sentence.
   - When `ok` is false, the sentence states the reason briefly in {support_language}.

3. "ok" — boolean. `true` if the item belongs to the target language and can be turned into a card. `false` otherwise. When `false`, `understanding` must briefly state the reason.

Examples (target=en, support=ru) — shape only:

- term "cat", understanding "Базовое сущ. «кошка», бытовая лексика."
- term "to boot", understanding "Идиома «в придачу» — добавочная выгода поверх основной."
- term "harbor", understanding "Переносный гл. «носить в себе чувство», не порт."
- term "сообщение", ok false, understanding "Слово на русском, не на target-языке — карточка не создаётся."

Return only JSON with this shape, no prose, no code fences:

{"target_lang":"<iso code of {target_language}>","items":[{"term":"<surface form>","understanding":"<one sentence in the support language>","ok":true}]}

Current rows:
{current_rows}

User correction:
{user_correction}
