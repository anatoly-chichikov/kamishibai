You are refining the Kamishibai understanding list after a user comment.

Target language:
{target_language}

Support language for explanations:
{support_language}

Apply the user's correction to the whole list while keeping the target language fixed. Keep rows that should not be turned into cards with `"ok": false` and explain why in their `understanding`.

Each item has three fields:

1. "term" — the surface form the next step will use. Correct typos silently if needed. Preserve morphological form unless the user's correction explicitly asks otherwise.

2. "understanding" — one short, scannable line in {support_language}, structured as `<part of speech abbreviation> · <gloss>[ · <decision marker if any>]`.

   The entire line — POS, gloss, decision marker — must be written in {support_language}. Never mix English POS labels into a non-English line. Pick the right abbreviation set for {support_language}:
   - Russian (`ru`): `сущ.`, `гл.`, `прил.`, `нареч.`, `мест.`, `союз`, `предл.`, `част.`, `межд.`, `числ.`; phrases `фраза`; idioms `идиома`; collocations `колл.`; full sentences `предл.`.
   - English (`en`): `noun`, `verb`, `adj.`, `adv.`, `pron.`, `conj.`, `prep.`, `part.`, `interj.`, `num.`; phrases `phrase`; idioms `idiom`; collocations `collocation`; full sentences `sentence`.
   - Spanish (`es`): `sust.`, `verbo`, `adj.`, `adv.`, `pron.`, `conj.`, `prep.`, `part.`, `interj.`, `num.`; phrases `frase`; idioms `idiom.`; collocations `coloc.`; full sentences `oración`.
   - German (`de`): `Subst.`, `Verb`, `Adj.`, `Adv.`, `Pron.`, `Konj.`, `Präp.`, `Part.`, `Interj.`, `Num.`; phrases `Phrase`; idioms `Idiom`; collocations `Kollok.`; full sentences `Satz`.
   - Greek (`el`): `ουσ.`, `ρ.`, `επίθ.`, `επίρρ.`, `αντων.`, `σύνδ.`, `πρόθ.`, `μόρ.`, `επιφ.`, `αριθμ.`; phrases `φράση`; idioms `ιδιωτ.`; collocations `συνεμφ.`; full sentences `πρόταση`.
   - Chinese (`zh`): `名`, `动`, `形`, `副`, `代`, `连`, `介`, `助`, `叹`, `数`; phrases `短语`; idioms `成语`; collocations `搭配`; full sentences `句子`.
   - For other support languages, use that language's standard short POS labels — never fall back to English.

   The gloss is one word or a couple of comma-separated translations in {support_language}, never a full sentence.

   Add a third segment after ` · ` only when a real decision applies — chosen polysemous sense (with what was rejected), non-default morphology, typo correction, non-neutral register or style, idiom/collocation note, or the reason for exclusion when `ok` is false.

   Hard prohibitions:
   - Never use empty filler words like "обычное", "обычный", "повседневное", "стандартное", "обыкновенное", "regular", "common", "ordinary", "everyday", "standard", "basic", "simple". Drop them.
   - Do not describe the obvious semantic field a user already knows (e.g., "одомашненное животное", "цвет", "день недели").
   - Do not write a full prose sentence. Stick to the `<pos> · <gloss>[ · <marker>]` shape with ` · ` separators.

   Examples for an EN→RU pair: `сущ. · кошка`, `прил. · красный`, `союз · пока, в то время как · формальный британский`, `гл. · дебютировать · прошедшее время`, `гл. · исключить · из учебного заведения, не «выпустить газ»`, `пропуск · слово на русском, не на target-языке`.

   Do not introduce labels, tags, or structured fields beyond the dot-separated segments.

3. "ok" — boolean. `true` if the item belongs to the target language and can be turned into a card; `false` otherwise. When `false`, `understanding` must state the reason briefly.

Return only JSON with this shape, no prose, no code fences:

{"target_lang":"<iso code>","items":[{"term":"<surface form>","understanding":"<one or two sentences in the support language>","ok":true}]}

Current rows:
{current_rows}

User correction:
{user_correction}
