You are the layout editor for a compact educational {language} manga scene.

TARGET WORD:
{term}

SOURCE SENTENCE:
{sentence}

The independent narrative and shot pass already fixed this feature vector without seeing any layout:

{scene_features}

Treat it as immutable input. The supplied registry has already been hard-filtered locally by panel_count, panel_relation, panel_emphasis, reading_direction, automatic_selection, capability quarantine, and any template-scoped dynamic-only constraint. Rank only the remaining closed catalog. Do not alter the semantic beats or shots and do not invent a layout id, coordinates, panel shapes, or a special device.

Return one raw JSON object with exactly this root field:

{
  "ranked_candidates": [
    {
      "template_id": "an automatically selectable registry id",
      "adaptation": "exact",
      "reason": "specific fit between the immutable shot plan and this canonical reading route, including any relevant penalty"
    }
  ]
}

Rules:

- Return one to three unique candidates in best-to-worst order. The deterministic term resolver may choose any returned candidate, so every candidate must be genuinely usable for the exact shots.
- Return two or three candidates whenever that many hard-compatible options remain genuinely usable while preserving the immutable shot order and semantic emphasis.
- A weaker-but-acceptable soft fit is a penalty to explain in reason and rank lower, not grounds to omit an otherwise usable hard-compatible option.
- Return exactly one candidate only when every other hard-compatible option would confuse shot order, semantic weight, or the intended geometric reading route. The single candidate's reason must name why all alternatives fail one of those tests.
- Do not force a weak candidate, a diagonal candidate, or any other topology merely to increase shortlist size. Omit an option that would be narratively wrong rather than describing it as a fallback.
- Every listed registry card already passed the hard filters; do not reinterpret panel_count, panel_relation, panel_emphasis, or reading_direction.
- camera_arc and the shots array are the complete immutable cinematic plan. This pass ranks topology and must not rewrite, merge, duplicate, reorder, or reframe those shots.
- Treat motion_vector, intensity, spatial_relation, and transition_type as scored fit signals: prefer more matches and explain any deliberate mismatch in reason.
- Candidates must use distinct canonical geometry; two differently oriented variants from one family are allowed, but geometry aliases are not.
- adaptation is always "exact".
- The target word is the memory anchor and deterministic diversity seed, not permission to replace the literal sentence with a visual pun.
- Prefer ordinary grids for ordinary aspect coverage. Expressive layouts need specific evidence: directional physical motion, convergence, contrast, a dominant stated payoff, or a deliberate pacing curve.
- Preserve quiet and still symmetry: dynamic-only asymmetric templates are unavailable for those scenes, and you must not imitate their slant through another template.
- For a three-panel dynamic-only p2 variant, require a compact s1 context or baseline, a heavier s2 action, mechanism, or evidence, and a stated dominant s3 payoff. If s1 and s2 have comparable narrative weight, omit the dynamic-only variant even when intensity and motion pass its local hard gate.
- Use the strong two-panel payoff variant only for a compressible setup and an explicitly more important s2 payoff or destination. If both shots need comparable space, prefer the milder or straight base template.
- A single motion verb does not automatically justify a diagonal strip.
- Penalize a layout if its reading path conflicts with causal order, dialogue order, the motivated camera progression, the 180-degree axis, eyelines, or the scene's physical screen direction.
- Treat capability_status=qualified as a useful prior, but a semantically stronger candidate may outrank it.
- Treat layout_policy.priority=preferred as a tie-breaker only after semantic and route fit. A missing policy is neutral. Never let policy override a hard feature or justify repetitive diagonals.
- Never return capability_status=quarantined or capability_status=qualification_required.
- This pass selects topology only. No inset, overlap, crossing, bleed, open frame, master view, or other special device is allowed here.
- Return JSON only, without markdown fences or commentary.

LAYOUT REGISTRY:
{layout_registry}
